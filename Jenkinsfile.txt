node {

	stage('Git_clone') {
		echo 'cloning....'
        sh 'git clone 
	}

	stage('Terraform') {
		echo 'Terraform init'
        sh 'terraform init'
        sh 'terraform plan --var-file test.tfvars'
        
	}

	stage('Deploy') {
		echo 'Deploying....'
        sh 'terraform apply --var-file test.tfvars'
	}
}
